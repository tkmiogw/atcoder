#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<ll, ll>;
const ll INF = 100000000;
#define rep(i, n) for (ll i=0; i<(n); i++) 


int main() {
    ll n, k;
    cin >> n >> k;
    rep(i, n) cin >> a[i];    
    cout << "Hello, world!" << endl;
}