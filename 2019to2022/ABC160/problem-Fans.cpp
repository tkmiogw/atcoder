#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i<(n); i++) 
using namespace std;
using ll = long long;
using P = pair<int, int>;
const int INF = 100000000;


int main() {
    int n;
    cin >> n;
    vector<int> a(n), b(n);
    rep(i, n){
        cin >> a[i];
        cin >> b[i];
    }
    
    cout << "Hello, world!" << endl;
}