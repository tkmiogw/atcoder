#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i<(n); i++) 
using namespace std;
using ll = long long;
using P = pair<int, int>;
const int INF = 100000000;

int main() {
    string s, t;
    cin >> s >> t;
    ll int n;
    cin >> n;
    vector<int> a(n, 0);
    rep(i, n) cin >> a[i];    
    cout << "Hello, world!" << endl;
}