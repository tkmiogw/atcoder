A
other answer
#include<cstdio>
int a,b,c;
int main()
{
	scanf("%d%d%d",&a,&b,&c);
	puts(a+b+c>=22?"bust":"win");
	return 0;
}
#なぜかコンパイルエラーが入っているな?
#scanf(型の指定, 振り分ける文字)
#printf(pythonと同じ)

B
.lengthだけではなく、sizeでも長さは返せる
実行時間そこまで変わらなさそうだから、halfはいらないかも

C
テーマはbit全探索のことらしい。
実装がシンプルに難しかった。参考になるコードがいっぱいあるよ
ご質問の件は以下のテストプログラムの結果でもわかる様に、if(a&b)の場合、aの結果にかかわらず、bが実行されます。それは上のサイトでも書いているように、aの結果にかかわらずa&bのビット演算するためにbを評価する必要があるためと思われます。

D
Aが2^60まである。これは64ビットでは表せない数字なので工夫が必要。(long long型という)
O(10^9)が限度
